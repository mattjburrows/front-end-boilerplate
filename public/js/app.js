/**
 * js > app.js
 * @author Matthew Burrows <matthew.burrows@essencedigital.com>
 */

// Sandbox the jQuery object to the $
( function ( $ ) {

	var $win = $(window),
		$doc = $(document),
		Ess  = Ess || {};

} )( jQuery );